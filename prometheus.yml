global:
  scrape_interval: 5s
  evaluation_interval: 5s
  external_labels:
    monitor: 'demo-monitor'

scrape_configs:
  - job_name: 'api-simulator'
    static_configs:
      - targets: ['app:8000']
    metrics_path: /metrics
